import React from 'react';
import {DateTimeInput} from '../../../src/core';
import sinon from 'sinon';
import {mount} from 'enzyme';
import helper from '../../helper';
describe('(Component) DateTimeInput', function () {

    it('renders correctly', function () {

        let id = helper.generateRandomString(helper.generateRandomNautralNumber());

        // let wrapper = mount(<DateTimeInput id={id}/>);
        //
        // let div = wrapper.find('div');
        //
        // div.length.should.be.equal(1);
        //
        // div.props().id.should.be.equal(id);

    });

});


